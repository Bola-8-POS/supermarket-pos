---
phase: 34-tutorial-video-generation
plan: 05
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - e2e/tutorials/settings/settings.spec.ts
  - e2e/tutorials/audit/audit.spec.ts
autonomous: true
requirements: [VIDEO-02, VIDEO-04, VIDEO-06]

estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "A store owner watching tutorial-videos/settings/*.mp4 sees the real Settings -> Language self-service switch, not a DB-seeded shortcut"
    - "A store owner watching tutorial-videos/audit/*.mp4 sees the audit log populate after a payment and a diff sheet open"
  artifacts:
    - e2e/tutorials/settings/settings.spec.ts
    - e2e/tutorials/audit/audit.spec.ts
  key_links:
    - "settings.spec.ts is the ONE domain that drives the real Settings -> Language UI switch instead of calling seedStaffLocale() (per 34-RESEARCH.md Pattern 2/Anti-Patterns) — bilingual proof for every OTHER domain comes from seedStaffLocale(), this one demonstrates the mechanism itself"
    - "audit.spec.ts imports test/expect from ../fixtures (34-01) and seedStaffLocale/currentTutorialLocale from ../locale (34-01)"
---

<objective>
Stamp out the final two per-domain tutorial videos (settings, audit). Settings is the one deliberate
exception to the DB-seeded-locale pattern used everywhere else in this phase: its own content IS the
Settings -> Language self-service switch, so it must drive that real UI control rather than shortcut
around it. Audit follows the standard pattern from Plans 34-02/34-03/34-04.

Purpose: complete VIDEO-02's 12-domain coverage (this plan finishes the set alongside 34-02/34-03/34-04).
Output: two new spec files.

LOCALE SAFETY (carried over from 34-01, do not skip — applies fully to `audit.spec.ts`; `settings.spec.ts`
is the deliberate exception described in Task 1 below): this harness's default `TUTORIAL_LOCALE` is
`es-MX`, but the CI-critical suite's E2E fixture accounts default to `en-US`
(`scripts/lib/ensure-staff-account.ts`) — so English-only text selectors in cited `e2e/**/*.spec.ts`
reference files will NOT match once `seedStaffLocale()` puts the acting staff into `es-MX`. Reuse
`e2e/tutorials/i18n-selectors.ts` (34-01) for cross-domain strings in `audit.spec.ts`; for a string
unique to this domain, look up its real es-MX/en-US values in
`src/shared/lib/i18n/locales/{es-MX,en-US}/*.json` and build a dual-locale regex inline, mirroring
`e2e/helpers/auth.ts`'s `WHO_ARE_YOU_RE`/`LOGOUT_RE` convention.

No one-way-door decisions (REVERSIBILITY_GATES): purely additive spec files reusing an already-proven
harness.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-tutorial-video-generation/34-RESEARCH.md
@e2e/tutorials/fixtures.ts
@e2e/tutorials/locale.ts
@e2e/tutorials/i18n-selectors.ts
@e2e/tutorials/checkout/checkout.spec.ts
@e2e/helpers/auth.ts
</context>

<threat_model>
## Trust Boundaries

Same as Plan 34-01 — reused unchanged, no new trust boundary introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-01 | Information Disclosure | New domain specs' captions/console output | medium | mitigate | Inherited from 34-01 |
| T-34-02 | Tampering | `seedStaffLocale()` call from `audit.spec.ts` | high | mitigate | Inherited from 34-01. `settings.spec.ts` does NOT call `seedStaffLocale()` at all (it drives the real UI switch instead), so T-34-02 does not apply to that spec |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Settings tutorial video (real locale-switch UI, not DB-seeded)</name>
  <files>e2e/tutorials/settings/settings.spec.ts</files>
  <read_first>e2e/tutorials/checkout/checkout.spec.ts, e2e/tutorials/fixtures.ts, e2e/settings/i18n-locale-switch.spec.ts (the `switchOwnLocale()` helper — real Settings -> Language flow, es-MX <-> en-US labels), e2e/settings/unsaved-changes-guard.spec.ts (dirty-tab exit-guard prompt)</read_first>
  <precondition>Local Supabase stack (55321-55323) running with `.env.local` E2E_* credentials present.</precondition>
  <action>
    Create `e2e/tutorials/settings/settings.spec.ts` mirroring `checkout.spec.ts`'s import shape EXCEPT:
    do NOT import or call `seedStaffLocale`/`currentTutorialLocale` from `'../locale'` in this file — this
    is the one domain (per 34-RESEARCH.md Pattern 2/Anti-Patterns) whose content IS the real language
    switch, so seeding around it would record nothing meaningful. Log in as `'admin'`. Test 1 —
    `'admin switches their own locale via Settings -> Language'`: adapt `i18n-locale-switch.spec.ts`'s
    `switchOwnLocale()` logic inline (open `/settings`, click the Language tab, open the locale
    `<select>`, choose the other locale's option, save), narrating each step and narrating the
    heading/tab text visibly re-rendering in the new language WITHOUT a page reload — this is the
    actual "bilingual mechanism" being demonstrated on screen, distinct from every other domain's
    DB-seeded approach. Switch back to `es-MX` at the end of the test (mirror
    `i18n-locale-switch.spec.ts`'s own cleanup) so the shared admin profile isn't left polluted. Test 2
    — `'a dirty settings tab prompts Save/Discard/Stay on navigation away'`: reproduce
    `unsaved-changes-guard.spec.ts`'s flow (make an edit on any settings tab, attempt to navigate away,
    narrate the Save/Discard/Stay dialog appearing), narrating each step. NOTE: Test 2 runs after Test 1
    has switched the admin's locale back to `es-MX`, so — unlike Test 1, which is inherently
    locale-aware by design — Test 2's Save/Discard/Stay selectors DO need to be locale-safe per this
    plan's LOCALE SAFETY note above (look up the real button labels in
    `src/shared/lib/i18n/locales/{es-MX,en-US}/settings.json` and build dual-locale regex).
    STOP-AND-ASK (VIDEO-06): if reproducing either flow needs more than ~3 debugging attempts beyond
    reusing the cited specs' selector NAMES/testids with their text-matching regex rebuilt as
    dual-locale (see `e2e/tutorials/i18n-selectors.ts` and 34-01's note on why English-only regex
    doesn't match es-MX), stop and report the exact blocker.
  </action>
  <verify>
    <automated>npx playwright test e2e/tutorials/settings --config=playwright.tutorial.config.ts</automated>
    <fails_when>Either test fails, times out, or the command exits non-zero; or fewer than 2 `.webm` files exist under `e2e-results-tutorials/raw/settings/` afterward.</fails_when>
  </verify>
  <done>Two passing recordings exist under `e2e-results-tutorials/raw/settings/`, with Test 1 demonstrating the real es-MX -> en-US -> es-MX switch on screen.</done>
  <acceptance_criteria>
    - `e2e/tutorials/settings/settings.spec.ts` exists and contains no import of `'../locale'` (grep confirms — this file deliberately does not use `seedStaffLocale`).
    - Running the verify command produces exactly 2 non-empty `.webm` files under `e2e-results-tutorials/raw/settings/`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Audit tutorial video</name>
  <files>e2e/tutorials/audit/audit.spec.ts</files>
  <read_first>e2e/tutorials/checkout/checkout.spec.ts, e2e/tutorials/fixtures.ts, e2e/tutorials/locale.ts, e2e/audit/audit-logs.spec.ts (view entries after a payment, open the diff sheet on a row click), e2e/audit/entity-id-crosslink.spec.ts (date-range filter / cross-entity link, for the edge case)</read_first>
  <precondition>Local Supabase stack (55321-55323) running with `.env.local` E2E_* credentials present.</precondition>
  <action>
    Create `e2e/tutorials/audit/audit.spec.ts` mirroring `checkout.spec.ts`'s shape, logging in as
    `'admin'` (`/audit` is admin-gated). Per RESEARCH.md Pitfall 3, `beforeEach` must produce a real
    audit-log entry itself: open a caja session, complete one real checkout as unnarrated setup (reuse
    the cash-sale steps from `e2e/tutorials/checkout/checkout.spec.ts` Test 1, logging back in as
    `'admin'` for the narrated portion). Test 1 — `'admin reviews the audit log and opens a diff
    sheet'`: reproduce `audit-logs.spec.ts`'s flow (navigate to `/audit`, locate the entry generated by
    the setup checkout, click it to open the diff sheet), narrating each step. Test 2 — `'admin
    narrows results with a date-range filter'`: reproduce the date-range-filter portion of
    `entity-id-crosslink.spec.ts` (or `audit-logs.spec.ts` if the filter lives there instead — read
    both before choosing), narrating the filter narrowing the visible entries.
    STOP-AND-ASK (VIDEO-06): if reproducing either flow needs more than ~3 debugging attempts beyond
    reusing the cited specs' selector NAMES/testids with their text-matching regex rebuilt as
    dual-locale (see `e2e/tutorials/i18n-selectors.ts` and 34-01's note on why English-only regex
    doesn't match es-MX), stop and report the exact blocker.
  </action>
  <verify>
    <automated>npx playwright test e2e/tutorials/audit --config=playwright.tutorial.config.ts</automated>
    <fails_when>Either test fails, times out, or the command exits non-zero; or fewer than 2 `.webm` files exist under `e2e-results-tutorials/raw/audit/` afterward.</fails_when>
  </verify>
  <done>Two passing es-MX audit recordings exist under `e2e-results-tutorials/raw/audit/`.</done>
  <acceptance_criteria>
    - `e2e/tutorials/audit/audit.spec.ts` exists, imports `test`/`expect` from `'../fixtures'`.
    - Running the verify command produces exactly 2 non-empty `.webm` files under `e2e-results-tutorials/raw/audit/`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `npx playwright test e2e/tutorials/settings e2e/tutorials/audit --config=playwright.tutorial.config.ts` — all 4 tests pass headless.
2. `npm run typecheck` and `npm run lint` both pass on the 2 new spec files.
</verification>

<success_criteria>
- The final two domains (settings, audit) have real, passing recording scripts, completing VIDEO-02's
  full 12-domain set across Plans 34-01 through 34-05.
</success_criteria>

<output>
Create `.planning/phases/34-tutorial-video-generation/34-05-SUMMARY.md` when done
</output>
