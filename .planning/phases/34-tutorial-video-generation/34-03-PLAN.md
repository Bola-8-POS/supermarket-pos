---
phase: 34-tutorial-video-generation
plan: 03
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - e2e/tutorials/staff-rbac/staff-rbac.spec.ts
  - e2e/tutorials/caja/caja.spec.ts
  - e2e/tutorials/reports/reports.spec.ts
autonomous: true
requirements: [VIDEO-02, VIDEO-04, VIDEO-06]

estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "A store owner watching tutorial-videos/staff-rbac/*.mp4 sees a new staff member added with a forced PIN change, and a non-admin denied /rbac"
    - "A store owner watching tutorial-videos/caja/*.mp4 sees a caja opened and closed, and the open-tabs guard on close"
    - "A store owner watching tutorial-videos/reports/*.mp4 sees a closed session's revenue/cash-reconciliation and a CSV export"
  artifacts:
    - e2e/tutorials/staff-rbac/staff-rbac.spec.ts
    - e2e/tutorials/caja/caja.spec.ts
    - e2e/tutorials/reports/reports.spec.ts
  key_links:
    - "Each spec imports test/expect from ../fixtures (34-01) and seedStaffLocale/currentTutorialLocale from ../locale (34-01) — zero new harness code in this plan"
---

<objective>
Stamp out three more domain tutorial videos (staff/rbac combined per REQUIREMENTS.md VIDEO-02 wording
and the existing `e2e/rbac/` folder precedent, caja, reports) using the exact harness proven by Plan
34-01 — no new harness/config/conversion code, only new domain spec files.

Purpose: continue VIDEO-02 coverage expansion in parallel with Plans 34-02/34-04/34-05 (no file
overlap between any of these four plans).
Output: three new spec files, each producing 2 `.webm` recordings.

LOCALE SAFETY (carried over from 34-01, do not skip): this harness's default `TUTORIAL_LOCALE` is
`es-MX`, but the CI-critical suite's E2E fixture accounts default to `en-US`
(`scripts/lib/ensure-staff-account.ts`) — so the English-only text selectors in every cited
`e2e/**/*.spec.ts` reference file will NOT match once `seedStaffLocale()` puts the acting staff into
`es-MX`. Reuse `e2e/tutorials/i18n-selectors.ts` (34-01) for cross-domain strings; for a string
unique to one of these three domains, look up its real es-MX/en-US values in
`src/shared/lib/i18n/locales/{es-MX,en-US}/*.json` (grep the component for its `t('namespace.key')`
call) and build a dual-locale regex inline, mirroring `e2e/helpers/auth.ts`'s `WHO_ARE_YOU_RE`/
`LOGOUT_RE` convention (`/^(english|spanish)$/i`) — never copy an English-only regex from a cited CI
spec verbatim. Prefer an existing `data-testid` (locale-independent) wherever the cited spec already
provides one.

No one-way-door decisions (REVERSIBILITY_GATES): purely additive spec files reusing an already-proven
harness.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-tutorial-video-generation/34-RESEARCH.md
@e2e/tutorials/fixtures.ts
@e2e/tutorials/locale.ts
@e2e/tutorials/i18n-selectors.ts
@e2e/tutorials/checkout/checkout.spec.ts
@e2e/helpers/auth.ts
</context>

<threat_model>
## Trust Boundaries

Same as Plan 34-01 — reused unchanged, no new trust boundary introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-01 | Information Disclosure | New domain specs' captions/console output | medium | mitigate | Inherited from 34-01 |
| T-34-02 | Tampering | `seedStaffLocale()` calls from these 3 new specs | high | mitigate | Inherited from 34-01 |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Staff/RBAC tutorial video</name>
  <files>e2e/tutorials/staff-rbac/staff-rbac.spec.ts</files>
  <read_first>e2e/tutorials/checkout/checkout.spec.ts, e2e/tutorials/fixtures.ts, e2e/tutorials/locale.ts, e2e/rbac/staff-management.spec.ts (SM2 forced-PIN-change-on-first-login flow, SM9 admin resets another staff member's PIN), e2e/rbac/rbac.spec.ts (non-admin redirected from /rbac)</read_first>
  <precondition>Local Supabase stack (55321-55323) running with `.env.local` E2E_* credentials present.</precondition>
  <action>
    Create `e2e/tutorials/staff-rbac/staff-rbac.spec.ts` mirroring `checkout.spec.ts`'s shape, logging
    in as `'admin'` for the staff-management scenario (adding/resetting staff is admin-scoped per
    `CLAUDE.md`'s RBAC table). Test 1 — `'admin adds a new staff member with a forced PIN change'`:
    reproduce `staff-management.spec.ts`'s SM2 flow (open `/staff`, Add Staff dialog, fill name/role/PIN,
    submit, then narrate the new staff member's first login being forced through the PIN-change
    screen), narrating each step. Test 2 — `'a cashier is redirected away from the RBAC page'`:
    reproduce `rbac.spec.ts`'s non-admin-redirect assertion — log in as `'cashier'` inside this second
    test (a fresh `beforeEach` login, not a role switch mid-test), navigate to `/rbac`, narrate the
    redirect happening.
    STOP-AND-ASK (VIDEO-06): if reproducing either flow needs more than ~3 debugging attempts beyond
    reusing the cited specs' selector NAMES/testids with their text-matching regex rebuilt as
    dual-locale (see `e2e/tutorials/i18n-selectors.ts` and 34-01's note on why English-only regex
    doesn't match es-MX), stop and report the exact blocker.
  </action>
  <verify>
    <automated>npx playwright test e2e/tutorials/staff-rbac --config=playwright.tutorial.config.ts</automated>
    <fails_when>Either test fails, times out, or the command exits non-zero; or fewer than 2 `.webm` files exist under `e2e-results-tutorials/raw/staff-rbac/` afterward.</fails_when>
  </verify>
  <done>Two passing es-MX staff-rbac recordings exist under `e2e-results-tutorials/raw/staff-rbac/`.</done>
  <acceptance_criteria>
    - `e2e/tutorials/staff-rbac/staff-rbac.spec.ts` exists, imports `test`/`expect` from `'../fixtures'`.
    - Running the verify command produces exactly 2 non-empty `.webm` files under `e2e-results-tutorials/raw/staff-rbac/`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Caja tutorial video</name>
  <files>e2e/tutorials/caja/caja.spec.ts</files>
  <read_first>e2e/tutorials/checkout/checkout.spec.ts, e2e/tutorials/fixtures.ts, e2e/tutorials/locale.ts, e2e/caja/session-management.spec.ts (open-with-float / close-at-end-of-day flow), e2e/caja/entries.spec.ts (cash-expense-entry registration and the cannot-close-with-open-tabs guard)</read_first>
  <precondition>Local Supabase stack (55321-55323) running with `.env.local` E2E_* credentials present.</precondition>
  <action>
    Create `e2e/tutorials/caja/caja.spec.ts` mirroring `checkout.spec.ts`'s shape, logging in as
    `'manager'` (caja session management is manager+). Do NOT call the shared `openCaja()` helper in
    `beforeEach` for this domain — the caja open/close flow IS the content being recorded, so drive it
    through the real UI instead. Test 1 — `'manager opens and closes a caja session'`: reproduce
    `session-management.spec.ts`'s open-with-drawer-float flow, register one cash entry (reuse
    `entries.spec.ts`'s registration flow), then close the session at end of day, narrating each step.
    Test 2 — `'caja cannot be closed while a tab is open'`: seed one open tab (reuse `seedOpenTab()`
    from `'../../helpers/supabase'`), open a caja session via the UI, attempt to close it, and narrate
    the blocking guard message.
    STOP-AND-ASK (VIDEO-06): if reproducing either flow needs more than ~3 debugging attempts beyond
    reusing the cited specs' selector NAMES/testids with their text-matching regex rebuilt as
    dual-locale (see `e2e/tutorials/i18n-selectors.ts` and 34-01's note on why English-only regex
    doesn't match es-MX), stop and report the exact blocker.
  </action>
  <verify>
    <automated>npx playwright test e2e/tutorials/caja --config=playwright.tutorial.config.ts</automated>
    <fails_when>Either test fails, times out, or the command exits non-zero; or fewer than 2 `.webm` files exist under `e2e-results-tutorials/raw/caja/` afterward.</fails_when>
  </verify>
  <done>Two passing es-MX caja recordings exist under `e2e-results-tutorials/raw/caja/`.</done>
  <acceptance_criteria>
    - `e2e/tutorials/caja/caja.spec.ts` exists, imports `test`/`expect` from `'../fixtures'`.
    - Running the verify command produces exactly 2 non-empty `.webm` files under `e2e-results-tutorials/raw/caja/`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Reports tutorial video</name>
  <files>e2e/tutorials/reports/reports.spec.ts</files>
  <read_first>e2e/tutorials/checkout/checkout.spec.ts, e2e/tutorials/fixtures.ts, e2e/tutorials/locale.ts, e2e/reports/report-tabs.spec.ts (pick a closed session, revenue/cash-reconciliation, Product Sales tab), e2e/reports/export.spec.ts (CSV export flow) — note RESEARCH.md Pitfall 3: Reports needs its own visible historical data, do not assume leftover rows from a prior run</read_first>
  <precondition>Local Supabase stack (55321-55323) running with `.env.local` E2E_* credentials present.</precondition>
  <action>
    Create `e2e/tutorials/reports/reports.spec.ts` mirroring `checkout.spec.ts`'s shape, logging in as
    `'admin'` (Reports is admin-facing per the routes table). Per RESEARCH.md Pitfall 3, `beforeEach`
    must create real visible history first: open a caja session (`openCaja()`), complete at least one
    real checkout (reuse the same cash-sale steps as `e2e/tutorials/checkout/checkout.spec.ts` Test 1,
    without the narration wrapper since this is setup, not the recorded content), close the caja
    session, THEN log out and log back in as `'admin'` for the narrated portion. Test 1 — `'admin
    reviews a closed session's revenue and product sales'`: reproduce `report-tabs.spec.ts`'s flow
    (navigate to `/reports`, pick the just-closed session, view the revenue/cash-reconciliation
    breakdown, switch to the Product Sales tab), narrating each step. Test 2 — `'admin exports a
    report to CSV'`: reproduce `export.spec.ts`'s CSV export flow (Payment Methods or Product Sales tab,
    whichever `export.spec.ts` actually exercises), narrating the export action and the resulting
    Tauri-native save-dialog mock confirmation.
    STOP-AND-ASK (VIDEO-06): if reproducing either flow needs more than ~3 debugging attempts beyond
    reusing the cited specs' selector NAMES/testids with their text-matching regex rebuilt as
    dual-locale (see `e2e/tutorials/i18n-selectors.ts` and 34-01's note on why English-only regex
    doesn't match es-MX), stop and report the exact blocker.
  </action>
  <verify>
    <automated>npx playwright test e2e/tutorials/reports --config=playwright.tutorial.config.ts</automated>
    <fails_when>Either test fails, times out, or the command exits non-zero; or fewer than 2 `.webm` files exist under `e2e-results-tutorials/raw/reports/` afterward.</fails_when>
  </verify>
  <done>Two passing es-MX reports recordings exist under `e2e-results-tutorials/raw/reports/`.</done>
  <acceptance_criteria>
    - `e2e/tutorials/reports/reports.spec.ts` exists, imports `test`/`expect` from `'../fixtures'`.
    - Running the verify command produces exactly 2 non-empty `.webm` files under `e2e-results-tutorials/raw/reports/`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `npx playwright test e2e/tutorials/staff-rbac e2e/tutorials/caja e2e/tutorials/reports --config=playwright.tutorial.config.ts` — all 6 tests pass headless.
2. `npm run typecheck` and `npm run lint` both pass on the 3 new spec files.
</verification>

<success_criteria>
- Three more domains (staff-rbac, caja, reports) have real, passing recording scripts using the
  unmodified 34-01 harness.
</success_criteria>

<output>
Create `.planning/phases/34-tutorial-video-generation/34-03-SUMMARY.md` when done
</output>
