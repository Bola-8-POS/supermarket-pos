---
phase: 34-tutorial-video-generation
plan: 06
type: execute
wave: 3
depends_on: ["34-02", "34-03", "34-04", "34-05"]
files_modified:
  - e2e/tutorials/full-walkthrough/full-walkthrough.spec.ts
autonomous: true
requirements: [VIDEO-03, VIDEO-04, VIDEO-05, VIDEO-06]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "A store owner watching tutorial-videos/full-walkthrough/*.mp4 sees a full business day: caja open -> cashier sale -> manager refund -> admin reports -> caja close"
    - "Every one of the 13 spec groups (12 domains + full-walkthrough) has produced both an es-MX and an en-US .mp4 under tutorial-videos/"
    - "tutorial-videos/ contains only non-empty, playable .mp4 files — no zero-byte or missing outputs"
  artifacts:
    - e2e/tutorials/full-walkthrough/full-walkthrough.spec.ts
    - tutorial-videos/**/*.mp4 (generated, gitignored — both locales, all 13 groups)
  key_links:
    - "full-walkthrough.spec.ts chains caja -> checkout -> payments(refund) -> reports role transitions via logout()/loginAs(), each step narrated and locale-seeded"
    - "TUTORIAL_LOCALE=es-MX and TUTORIAL_LOCALE=en-US full record runs -> scripts/tutorial-videos-convert.ts -> tutorial-videos/<domain>/*.<locale>.mp4 for all 13 groups"
---

<objective>
Deliver the phase's last two requirements: the single long end-to-end business-day video (VIDEO-03),
and the full bilingual double-run + final conversion across every domain script written in Plans
34-01 through 34-05 (VIDEO-04/VIDEO-05), with a phase-gate file-count verification proving nothing is
missing.

Purpose: close out the phase. This is the only plan that runs the FULL `npm run tutorial-videos`
pipeline against every spec group, in both locales — every earlier plan deliberately scoped its own
verify to just its own domain folder to keep runtimes bounded during development.
Output: `e2e/tutorials/full-walkthrough/full-walkthrough.spec.ts`, and the complete
`tutorial-videos/` tree. Test count: 12 domains x 2 tests each (Plans 34-01 through 34-05) + 1
full-walkthrough test (this plan) = 25 recordings per locale x 2 locales = 50 `.mp4` files total.

LOCALE SAFETY (carried over from 34-01): this harness's default `TUTORIAL_LOCALE` is `es-MX`; the CI
suite's fixture accounts default to `en-US`. The new `full-walkthrough.spec.ts` written in this plan
must use `e2e/tutorials/i18n-selectors.ts`'s dual-locale constants (it reuses checkout/payments/
reports steps that already have dual-locale selectors from earlier plans) — see 34-01 Task 1 for the
full rationale.

No one-way-door decisions (REVERSIBILITY_GATES): the full bilingual run only produces gitignored video
files; nothing here is committed to the repo as a build artifact, and the spec file itself is
trivially deletable/rerunnable.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-tutorial-video-generation/34-RESEARCH.md
@e2e/tutorials/fixtures.ts
@e2e/tutorials/locale.ts
@e2e/tutorials/i18n-selectors.ts
@e2e/tutorials/checkout/checkout.spec.ts
@e2e/helpers/auth.ts
</context>

<threat_model>
## Trust Boundaries

Same as Plan 34-01 — reused unchanged. The only new consideration is running the harness at full
scale (all 13 groups x 2 locales), which does not introduce a new trust boundary, only more volume
through the existing ones.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-01 | Information Disclosure | `full-walkthrough.spec.ts` captions/console output across 3 role logins | medium | mitigate | Inherited from 34-01 — role transitions use `logout()`/`loginAs()`, never print raw credentials |
| T-34-02 | Tampering | `seedStaffLocale()` calls across the full bilingual run | high | mitigate | Inherited from 34-01 — `seedStaffLocale()` itself refuses a non-local `VITE_SUPABASE_URL`, enforced identically at full scale |
| T-34-04 | Denial of Service (self) | Running all 26 recordings sequentially (`workers: 1`, `fullyParallel: false`) is long-running | low | accept | Deliberate — training-video quality (`e2e/training/` precedent) prioritizes a stable, non-interleaved recording over parallel speed; this is dev-tooling run on demand, not a CI gate with a latency SLA |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Full end-to-end business-day walkthrough video</name>
  <files>e2e/tutorials/full-walkthrough/full-walkthrough.spec.ts</files>
  <read_first>e2e/tutorials/checkout/checkout.spec.ts (cash-sale steps + i18n-selectors usage to reuse), e2e/tutorials/caja/caja.spec.ts (34-03, open-with-float steps to reuse), e2e/tutorials/payments/payments.spec.ts (34-02, refund steps to reuse), e2e/tutorials/reports/reports.spec.ts (34-03, revenue/reconciliation view steps to reuse), e2e/helpers/auth.ts (logout/loginAs for the role transitions), e2e/tutorials/i18n-selectors.ts</read_first>
  <precondition>Local Supabase stack (55321-55323) running with `.env.local` E2E_* credentials present. Plans 34-02 and 34-03 must be complete (this spec reuses their caja/payments/reports step sequences).</precondition>
  <action>
    Create `e2e/tutorials/full-walkthrough/full-walkthrough.spec.ts` mirroring `checkout.spec.ts`'s
    import shape. This is ONE continuous test (`'a full business day: open, sell, refund, report,
    close'`), not multiple `test()` blocks — VIDEO-03 asks for one long video, not several short ones,
    so keep it as a single `test()` with role transitions inline via `logout(page)` (from
    `'../../helpers/auth'`) followed by `loginAs(page, <nextRole>)`, calling `seedStaffLocale()` again
    for each newly-logged-in staff member before their portion (a fresh login can reset which staff
    row is "active" for locale purposes). Sequence, narrating every step exactly like the per-domain
    specs already do: (1) `'manager'` opens a caja session with a drawer float (reuse `caja.spec.ts`'s
    open-with-float step sequence, Plan 34-03); (2) `logout()` + `loginAs(page, 'cashier')`, complete
    one real cash sale (reuse `checkout.spec.ts` Test 1's step sequence verbatim, Plan 34-01); (3)
    `logout()` + `loginAs(page, 'manager')`, process a refund on that same sale with the manager PIN
    gate (reuse `payments.spec.ts`'s refund step sequence, Plan 34-02); (4) `logout()` + `loginAs(page,
    'admin')`, open Reports and view the day's revenue/cash-reconciliation reflecting the sale and the
    refund (reuse `reports.spec.ts`'s revenue-review step sequence, Plan 34-03); (5) `logout()` +
    `loginAs(page, 'manager')`, close the caja session at end of day (reuse `caja.spec.ts`'s close
    step). Set `test.setTimeout(300_000)` at the top of the test body (5 real workflows x holds is
    longer than the file-level `playwright.tutorial.config.ts` 180s default). Do not invent new
    selectors for any of these five steps — copy the exact locale-safe selector expressions already
    written in the cited Plan 34-01/34-02/34-03 spec files.
    STOP-AND-ASK (VIDEO-06): if chaining these five reused step-sequences together needs more than ~3
    debugging attempts beyond straightforward copy-paste (e.g. a role-transition timing race not seen
    in the individual domain specs), stop and report the exact blocker rather than iterating unattended.
  </action>
  <verify>
    <automated>npx playwright test e2e/tutorials/full-walkthrough --config=playwright.tutorial.config.ts</automated>
    <fails_when>The test fails, times out (including the raised 300s per-test timeout), or the command exits non-zero; or zero `.webm` files exist under `e2e-results-tutorials/raw/full-walkthrough/` afterward.</fails_when>
  </verify>
  <done>One passing es-MX full-walkthrough recording exists under `e2e-results-tutorials/raw/full-walkthrough/`.</done>
  <acceptance_criteria>
    - `e2e/tutorials/full-walkthrough/full-walkthrough.spec.ts` exists with exactly one `test()` covering all 5 steps.
    - Running the verify command produces exactly 1 non-empty `.webm` file under `e2e-results-tutorials/raw/full-walkthrough/`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Full bilingual double-run across all 13 spec groups</name>
  <files>e2e-results-tutorials/raw/** (generated output only, no source files modified)</files>
  <read_first>e2e/tutorials/fixtures.ts (page-fixture video.saveAs() teardown — confirms the `.webm` naming already embeds the locale from `process.env.TUTORIAL_LOCALE`), playwright.tutorial.config.ts (`fullyParallel: false`, `workers: 1` — confirms the two locale runs must be sequential, not parallel, or their `TUTORIAL_LOCALE` env values would race)</read_first>
  <precondition>Local Supabase stack (55321-55323) running with `.env.local` E2E_* credentials present. All of Plans 34-01 through 34-05 and this plan's Task 1 must be complete — this task runs every spec group written across the whole phase.</precondition>
  <action>
    This task performs no new file changes — it is the phase-gate execution proof. Run the full record
    pass twice, once per locale (sequential, not backgrounded — `playwright.tutorial.config.ts` is
    single-worker so a second concurrent run would corrupt the first's in-flight recordings): first
    with `TUTORIAL_LOCALE=es-MX npm run tutorial-videos:record`, confirm it exits 0, then with
    `TUTORIAL_LOCALE=en-US npm run tutorial-videos:record`, confirm it exits 0. If either run reports
    failures, apply the STOP-AND-ASK rule (VIDEO-06) from the plan that authored the failing spec file
    rather than patching an unrelated domain script from this task — fix forward only within the
    scope of what that domain's own plan already specified; if the fix is not obvious within ~3
    attempts, stop and report which spec/locale/step failed.
  </action>
  <verify>
    <automated>find e2e-results-tutorials/raw -name "*.webm" | wc -l</automated>
    <fails_when>Either `npm run tutorial-videos:record` invocation exits non-zero, or the total `.webm` count across both runs is not exactly 50 (25 recordings x 2 locales: 24 domain-test recordings from Plans 34-01 through 34-05 plus 1 full-walkthrough recording from this plan's Task 1, each produced once per locale).</fails_when>
  </verify>
  <done>Both locale record passes complete successfully; exactly 50 `.webm` files exist across `e2e-results-tutorials/raw/**`.</done>
  <acceptance_criteria>
    - `TUTORIAL_LOCALE=es-MX npm run tutorial-videos:record` exits 0.
    - `TUTORIAL_LOCALE=en-US npm run tutorial-videos:record` exits 0.
    - `find e2e-results-tutorials/raw -name "*.webm" | wc -l` reports exactly `50`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Final ffmpeg conversion + phase-gate file-count verification</name>
  <files>tutorial-videos/** (generated output only, no source files modified)</files>
  <read_first>scripts/tutorial-videos-convert.ts (34-01 — the conversion script this task runs, unmodified)</read_first>
  <action>
    This task performs no new file changes. Run `npm run tutorial-videos:convert` once (it globs
    `e2e-results-tutorials/raw/**/*.webm` recursively across BOTH locale subsets already produced by
    Task 2, so a single invocation converts everything). Then verify the resulting `tutorial-videos/`
    tree: exactly 50 `.mp4` files total, none of them 0 bytes, with a roughly even 25/25 split between
    `*.es-MX.mp4` and `*.en-US.mp4` suffixes. If the count is off, diagnose which specific domain/locale
    combination is missing (compare the `.webm` list from Task 2 against the produced `.mp4` list) and
    apply the STOP-AND-ASK rule (VIDEO-06) rather than silently accepting a partial result as "done."
  </action>
  <verify>
    <automated>npm run tutorial-videos:convert; find tutorial-videos -name "*.mp4" | wc -l; find tutorial-videos -name "*.mp4" -size 0 | wc -l</automated>
    <fails_when>`npm run tutorial-videos:convert` exits non-zero, the total `.mp4` count is not exactly 50, or the 0-byte-file count is anything other than `0`.</fails_when>
  </verify>
  <verify>
    <automated>find tutorial-videos -name "*.es-MX.mp4" | wc -l; find tutorial-videos -name "*.en-US.mp4" | wc -l</automated>
    <fails_when>Either count is not exactly `25`, indicating the bilingual run in Task 2 did not actually produce a complete, matched pair of recordings for every scenario in both locales.</fails_when>
  </verify>
  <done>`tutorial-videos/` contains exactly 50 non-empty `.mp4` files (25 `es-MX` + 25 `en-US`) across all 13 spec groups, ready to embed on the product website and hand to customers.</done>
  <acceptance_criteria>
    - `find tutorial-videos -name "*.mp4" | wc -l` reports exactly `50`.
    - `find tutorial-videos -name "*.mp4" -size 0 | wc -l` reports `0`.
    - `find tutorial-videos -name "*.es-MX.mp4" | wc -l` and the `en-US` equivalent each report `25`.
    - `tutorial-videos/full-walkthrough/` contains exactly 2 files (`*.es-MX.mp4` and `*.en-US.mp4`).
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `npx playwright test e2e/tutorials/full-walkthrough --config=playwright.tutorial.config.ts` — the single full-walkthrough test passes headless.
2. `TUTORIAL_LOCALE=es-MX npm run tutorial-videos:record && TUTORIAL_LOCALE=en-US npm run tutorial-videos:record` — both full bilingual passes exit 0.
3. `npm run tutorial-videos:convert` then `find tutorial-videos -name "*.mp4" | wc -l` — reports exactly `50`, zero of them 0 bytes.
4. `npm run typecheck` and `npm run lint` both pass on the new spec file.
5. `npx playwright test --list 2>&1 | grep -c "e2e/tutorials"` still returns `0` — the CI-critical suite remains unaffected by the phase's full file set.
</verification>

<success_criteria>
- VIDEO-03 (one long end-to-end business-day video) exists and passes.
- VIDEO-04 (bilingual coverage) is proven for every one of the 13 spec groups, not just the tracer.
- VIDEO-05 (ffmpeg-converted, correctly-pathed `.mp4` output under gitignored `tutorial-videos/`) is
  proven at full scale, matching the tracer's per-file proof from Plan 34-01.
- The phase's full `tutorial-videos/` deliverable exists and is ready to embed/hand to customers.
</success_criteria>

<output>
Create `.planning/phases/34-tutorial-video-generation/34-06-SUMMARY.md` when done
</output>
